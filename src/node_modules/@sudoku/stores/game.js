import { derived, writable } from 'svelte/store';
import { gameSession } from './game-session';

export const gamePaused = writable(true);
export const gameWon = derived(gameSession, ($gameSession) => $gameSession.solved, false);
